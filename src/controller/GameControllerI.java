package controller;

import model.dungeon.DungeonInterface;

/**
 * Interface controller which handles the control of the game.
 * */

public interface GameControllerI {

  /**
   * Execute a single dungeon game Model. When the game is over,
   * the playGame method ends.
   *
   */
  void playGame();






}